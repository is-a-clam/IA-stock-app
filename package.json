{
  "name": "stock-app-cs-ia",
  "homepage": ".",
  "license": "MIT",
  "version": "0.1.0",
  "engines": {
    "npm": "6.14.11"
  },
  "scripts": {
    "build": "npm build --prefix frontend/"
  },
  "scripts": {
    "build": "npm install --prefix frontend/ && npm build --prefix frontend/",
    "deploy": "cp -a frontend/build/. public/",
    "heroku-postbuild": "npm build && npm deploy"
  }
}
